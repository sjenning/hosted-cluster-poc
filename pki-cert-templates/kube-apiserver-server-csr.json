{
  "CN": "${user}",
  "hosts": [
    "kubernetes",
    "kubernetes.default.svc",
    "kubernetes.default.svc.cluster.local",
    "kube-apiserver",
    "kube-apiserver.${NAMESPACE}.svc",
    "kube-apiserver.${NAMESPACE}.svc.cluster.local",
    "${EXTERNAL_API_DNS_NAME}",
    "${EXTERNAL_PRIVATE_API_DNS_NAME}",
    "${EXTERNAL_API_DNS_NAME_EXTERNAL}",
    "${EXTERNAL_PRIVATE_API_DNS_NAME_EXTERNAL}",
    ${EXTERNAL_API_DNS_NAME_ENDPOINTS},
    ${EXTERNAL_PRIVATE_API_DNS_NAME_ENDPOINTS},
    "${EXTERNAL_API_IP_ADDRESS}",
    "${SERVICE_NETWORK_PREFIX}.0.1"
  ],
  "key": {
    "algo": "rsa",
    "size": 2048
  },
  "names": [
    {
      "C": "US",
      "L": "CA",
      "ST": "San Francisco",
      "O": "${org}"
    }
  ]
}
