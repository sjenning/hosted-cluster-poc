apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: etcd-operator-crdcreation-"${CLUSTER_ID}"
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: etcd-operator-crdcreation
subjects:
  - kind: ServiceAccount
    name: etcd-operator
    namespace: "${NAMESPACE}"
